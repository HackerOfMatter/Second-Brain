# Core — this is the whole system.
pydantic>=2.0
PyYAML>=6.0
httpx>=0.27
starlette>=0.37
uvicorn>=0.30

# Optional: Google Calendar sync (calendar.sink: google). The default .ics
# sink needs none of this.
# google-api-python-client>=2.100
# google-auth-oauthlib>=1.2
